in codebase folder we have the model classes, here itransformer can also be implemented.

right now each model pipeline class has 4 methods: preprocess for training, preprocess for inference, training and inference. Inference functions are not really tested too well yet.
i used main.py to test if the class runs. Datasets are in the data folder. Lastly, i have a preprocess folder where i added a function that i used as a helper for my classes.

i used python 3.12.3 on a linux machine for the venv
